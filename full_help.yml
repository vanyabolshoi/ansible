---
- name: Get full help
  hosts: all
  gather_facts: no
  tasks:
    - ansible.windows.win_shell: |
        $h = & "C:\capture\capture.exe" --help 2>&1
        foreach ($line in $h) { Write-Host $line }
        exit 0
      register: r
    - debug:
        var: r.stdout_lines
