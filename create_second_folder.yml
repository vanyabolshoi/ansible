$folderPath = "C:\Новая папка 6"

if (-Not (Test-Path -Path $folderPath)) {
    New-Item -Path $folderPath -ItemType Directory
    Write-Output "Папка создана: $folderPath"
} else {
    Write-Output "Папка уже существует: $folderPath"
}
