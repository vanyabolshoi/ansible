#Set-ExecutionPolicy Unrestricted -Scope CurrentUser
 $folderPath = "C:\Новая папка 8"
        if (-Not (Test-Path $folderPath)) {
          New-Item -Path $folderPath -ItemType Directory
        }
#Set-ExecutionPolicy Restricted -Scope CurrentUser
